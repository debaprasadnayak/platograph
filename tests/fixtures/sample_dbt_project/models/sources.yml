version: 2

sources:
  - name: raw
    description: Raw source tables
    tables:
      - name: orders
        description: Raw orders table
      - name: customers
        description: Raw customers table
